          <%= link_to movie_path(movie) do %>
            <%= attachment_image_tag movie, :image, size: "150x200", format: 'jpeg', fallback: "no_image.jpg" %>
          <% end %>
          <%= movie.title %>
          <%= movie.body %>
          <%= movie.genre.name %>

          <div class="favorite-btn"><%= render 'favorite', movie: movie %></div>