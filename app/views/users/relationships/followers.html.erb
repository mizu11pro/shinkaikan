<% if @users.exists? %>
    <h3>
      <% if @user == current_user %>
        あなたがフォローしています
      <% else %>
        <%= @user.name %>さんがフォローしています
      <% end %>
    </h3>
    <%= render 'relationship', users: @users %>
<% else %>
    <h3>フォローしているユーザは現在いません</h3>
<% end %>