	<%= link_to followers_path(user_id: user.id), class: "btn btn-outline-primary text-white" do %>
		<%= user.followings.count %>フォロー
	<% end %>
	<%= link_to followings_path(user_id: user.id), class: "btn btn-outline-primary text-white" do %>
		<%= user.followers.count %>フォロワー
	<% end %>